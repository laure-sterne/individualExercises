import os
import logging

class CompressOneImage:
    pass

class ProcessImages:

    # filenames will be a list of all files found in directory
    for (dirpath, dirnames, filenames) in os.walk(multithreading.bestImages):
        print(dirpath)
        print(dirnames)
        print(filenames)

logging.basicConfig(format="%(threadName)s:%(message)s")
